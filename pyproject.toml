[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
  "migrations",
  "!migrations/0001_initial.py",
]

[tool.ruff.lint]
select = ["E", "F", "I"]  # errors, fouten, import-sortering

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "F403"]